{
  "name": "surfboard-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: 订阅链接 → Surfboard 完整配置 (HY2 only)",
  "scripts": {
    "build": "node scripts/build.cjs",
    "test": "node test/run.js",
    "deploy": "npm run build && wrangler deploy",
    "lint": "prettier --check src/ test/ *.js",
    "lint:fix": "prettier --write src/ test/ *.js"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "prettier": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}